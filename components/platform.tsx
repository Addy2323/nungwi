'use client'

import { useEffect, useRef, useState } from 'react'
import Link from 'next/link'
import { ArrowRight, BarChart3, Bell, Box, ClipboardList, Heart, Home, Hotel, LogOut, MapPin, Menu, Package, Palmtree, Plus, Search, Settings, ShoppingBag, Tag, Truck, Users, Wallet, X } from 'lucide-react'
import { api, mutate } from '@/lib/client-api'
import type { Actor } from '@/lib/server/auth'
import PlatformForm, { type Field } from './platform-form'
import shell from '@/app/dashboard/dashboard.module.css'
import s from './platform.module.css'

type R=Record<string,any>
type FormSpec={title:string;action:string;fields:Field[];initial?:R}
const money=(n:number)=>`TZS ${Number(n||0).toLocaleString('en-US')}`
const stamp=(value:string)=>value?new Date(value).toLocaleString():''
const input=(name:string,label:string,type:Field['type']='text',more:Partial<Field>={}):Field=>({name,label,type,...more})
const options=(values:string[])=>values.map(value=>({value,label:value}))
const select=(name:string,label:string,choices:{value:string;label:string}[],more:Partial<Field>={})=>input(name,label,'select',{options:choices,...more})
const activeField=input('active','Active','checkbox')
function Table({rows,columns,empty='No records yet.'}:{rows:R[];columns:{label:string;render:(row:R)=>React.ReactNode}[];empty?:string}) {
  return !rows.length?<p className={s.empty}>{empty}</p>:<div className={s.scroll}><table className={s.table}><thead><tr>{columns.map(c=><th key={c.label}>{c.label}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={row.id||i}>{columns.map(c=><td key={c.label}>{c.render(row)}</td>)}</tr>)}</tbody></table></div>
}
const badge=(value:string)=><span className={`${s.badge} ${['Paid','Delivered','sent','delivered'].includes(value)?s.good:['Cancelled','Returned','failed','expired'].includes(value)?s.bad:s.badge}`}>{value}</span>

export default function Platform({user}:{user:Actor}) {
  const isStaff=['admin','sales','stock','delivery'].includes(user.role);const admin=user.role==='admin';const manager=user.role==='hotel_manager'
  const allowed=(roles:string[])=>roles.includes(user.role)
  const nav=isStaff?[{name:'Overview',icon:Home},...(['admin','sales','delivery'].includes(user.role)?[{name:'Orders',icon:ShoppingBag}]:[]),...(['admin','stock','sales'].includes(user.role)?[{name:'Products',icon:Box}]:[]),...(['admin','stock'].includes(user.role)?[{name:'Inventory',icon:Package}]:[]),...(admin?[{name:'Customers & Staff',icon:Users},{name:'Hotels',icon:Hotel},{name:'Commissions',icon:Wallet}]:[]),...(['admin','sales','delivery'].includes(user.role)?[{name:'Drivers',icon:Truck}]:[]),...(['admin','sales'].includes(user.role)?[{name:'Promotions',icon:Tag},{name:'Support',icon:ClipboardList}]:[]),{name:'Reports',icon:BarChart3},...(admin?[{name:'Notifications',icon:Bell},{name:'Audit',icon:ClipboardList},{name:'Settings',icon:Settings}]:[])]:[{name:'Overview',icon:Home},{name:'My Orders',icon:ShoppingBag},{name:'Favourites',icon:Heart},{name:'Addresses',icon:MapPin},{name:'Offers',icon:Tag},...(manager?[{name:'Hotel Staff',icon:Users},{name:'Commissions',icon:Wallet}]:[]),{name:'Account',icon:Settings},{name:'Support',icon:ClipboardList}]
  const [tab,setTab]=useState('Overview');const [menu,setMenu]=useState(false);const [data,setData]=useState<any>(null);const [catalog,setCatalog]=useState<R[]>([]);const [drivers,setDrivers]=useState<R[]>([]);const [hotels,setHotels]=useState<R[]>([]);const [account,setAccount]=useState<R|null>(null);const [error,setError]=useState('');const [notice,setNotice]=useState('');const [revision,setRevision]=useState(0);const [loading,setLoading]=useState(true);const [search,setSearch]=useState('');const [period,setPeriod]=useState('30');const [from,setFrom]=useState(new Date(Date.now()-30*86400000).toISOString().slice(0,10));const [to,setTo]=useState(new Date().toISOString().slice(0,10));const [form,setForm]=useState<FormSpec|null>(null);const [selected,setSelected]=useState<R|null>(null);const [inviteLink,setInviteLink]=useState('');const [reportType,setReportType]=useState(isStaff?user.role==='stock'?'stock':user.role==='delivery'?'deliveries':'sales':'statement');const [preview,setPreview]=useState<R|null>(null);const [calculator,setCalculator]=useState({eligible:100000,method:'percentage',value:10,tiers:'[{"threshold":0,"rate":10}]',amount:10000})
  const dialog=useRef<HTMLDialogElement>(null)
  const range={from:new Date(`${from}T00:00:00`).toISOString(),to:new Date(new Date(`${to}T00:00:00`).getTime()+86400000).toISOString()}
  const refresh=()=>setRevision(v=>v+1)
  useEffect(()=>{if(!['Overview','Orders','My Orders','Notifications'].includes(tab))return;const timer=setInterval(()=>{if(!document.hidden&&!form&&!selected)refresh()},10000);return()=>clearInterval(timer)},[tab,form,selected])
  const go=(name:string)=>{setTab(name);setMenu(false);setSearch('');setError('')}
  useEffect(()=>{let cancelled=false;setLoading(true);setError('');setData(null)
    const resources:Record<string,string>={'Overview':'overview','Orders':'orders','My Orders':'orders','Products':'products','Inventory':'inventory','Customers & Staff':'users','Hotel Staff':'users','Hotels':'hotels','Drivers':'drivers','Promotions':'promotions','Offers':'promotions-public','Commissions':'commissions','Notifications':'notifications','Audit':'audit','Settings':'settings','Account':'account','Addresses':'account','Favourites':'account','Support':isStaff?'support':'account','Reports':'overview'}
    Promise.all([api(resources[tab]||'overview',range),api<R[]>('catalogue'),api<R>('account'),allowed(['admin','sales','delivery'])?api<R[]>('drivers'):Promise.resolve([]),admin?api<R[]>('hotels'):Promise.resolve([])]).then(([main,products,profile,driverRows,hotelRows])=>{if(cancelled)return;setData(main);setCatalog(products);setAccount(profile);setDrivers(driverRows);setHotels(hotelRows)}).catch(e=>{if(!cancelled)setError(e.message)}).finally(()=>{if(!cancelled)setLoading(false)})
    return()=>{cancelled=true}
  // range is represented by its primitive dates to avoid refetching on every render.
  // eslint-disable-next-line react-hooks/exhaustive-deps
  },[tab,from,to,revision])
  useEffect(()=>{if(form||selected||inviteLink||preview){if(!dialog.current?.open)dialog.current?.showModal()}else dialog.current?.close()},[form,selected,inviteLink,preview])
  const close=()=>{setForm(null);setSelected(null);setInviteLink('');setPreview(null)}
  const execute=async(action:string,values:R={},message='Saved.')=>{setError('');try{const result=await mutate(action,values);setNotice(message);refresh();return result}catch(e){setError((e as Error).message);return null}}
  const saved=(value:any)=>{setForm(null);setNotice('Changes saved to the database.');if(value?.url)setInviteLink(value.url);if(selected)api('order',{id:selected.id}).then(setSelected).catch(e=>setError(e.message));refresh()}
  const productOptions=catalog.map(p=>({value:p.id,label:`${p.name} (${p.sku})`}))
  const editProduct=(row?:R)=>setForm({title:row?'Edit product':'Add product',action:'product.save',initial:row?{...row,active:!!row.active}:{active:true,category:'Soft drinks',unit:'bottle',unit_size:1,min_qty:1,reorder_level:10,deposit:0,cost:0,hotel_price:null,image:'/images/mango-coast.png'},fields:[input('name','Product name'),input('brand','Brand','text',{required:false}),input('category','Category'),input('description','Description','textarea',{required:false}),input('image','Image','text',{required:false}),input('volume','Size / volume','text',{required:false}),select('unit','Selling unit',options(['bottle','can','pack','carton','crate'])),input('unit_size','Base units per selling unit','number',{min:1,help:'Example: crate = 24 bottles. Batch quantities are always base units.'}),input('units','Additional selling units','json',{required:false,help:'Optional array of units with unit, unit_size, price, hotel_price and deposit. All selling units draw from the same base-unit batches.'}),input('sku','SKU / barcode'),input('cost','Default cost per base unit (TZS)','number'),input('price','Selling price per selling unit (TZS)','number'),input('hotel_price','Hotel selling price (TZS)','number',{required:false}),input('reorder_level','Reorder level (base units)','number'),input('min_qty','Minimum selling quantity','number',{min:1}),input('deposit','Returnable deposit per selling unit (TZS)','number'),activeField]})
  const inviteUser=()=>setForm({title:'Invite a user',action:'user.invite',initial:{role:manager?'hotel_staff':'customer',hotel_id:manager?user.hotel_id:null},fields:[input('name','Full name'),input('email','Email','email'),input('phone','International phone (+255…)'),select('role','Role',options(manager?['hotel_staff']:['customer','admin','sales','stock','delivery','hotel_manager','hotel_staff'])),...(manager?[]:[select('hotel_id','Hotel (hotel roles only)',hotels.map(h=>({value:h.id,label:h.name})),{required:false})])]})
  const hotelForm=(row?:R)=>setForm({title:row?'Edit hotel arrangement':'Add hotel',action:'hotel.save',initial:row?{...row,active:!!row.active}:{active:true,rule:{method:calculator.method,value:calculator.value,scope:'referral',tiers:calculator.method==='tiered'?JSON.parse(calculator.tiers):[]}},fields:[input('name','Hotel name'),input('contact','Contact person'),input('phone','International phone'),input('address','Address / landmarks'),input('instructions','Delivery instructions','textarea',{required:false}),input('code','Referral code'),input('rule','Commission arrangement','json',{help:'method: percentage, fixed or tiered; value: rate/amount; scope: referral, purchase or both. Tiers use per-order eligible sales thresholds. Excludes discount, delivery, tax and deposits.'}),activeField]})
  const promotionForm=(row?:R)=>setForm({title:row?'Edit promotion':'Create promotion',action:'promotion.save',initial:row?{...row,active:!!row.active}:{active:true,template:'banner',button:'Shop now',color:'#ff5405',audience:'all',kind:'percentage',value:15,starts_at:new Date().toISOString(),ends_at:new Date(Date.now()+7*86400000).toISOString(),product_ids:[]},fields:[input('title','Title'),input('code','Discount code'),input('offer_text','Offer copy','textarea'),input('image','Image URL / upload','text',{required:false}),input('color','Background colour (#hex)'),select('template','Template',options(['banner','card','badge'])),input('button','Button label'),input('product_ids','Linked product IDs','json',{help:`An empty list means all products. Available: ${catalog.map(p=>`${p.id} = ${p.name}`).join(', ')}`}),select('audience','Audience',options(['all','customer','hotel'])),select('kind','Discount method',options(['percentage','fixed'])),input('value','Percentage or fixed TZS amount','number'),input('starts_at','Starts','datetime-local'),input('ends_at','Ends','datetime-local'),activeField]})
  const driverForm=(row?:R)=>setForm({title:row?'Edit driver':'Save driver',action:'driver.save',initial:row?{...row,active:!!row.active}:{active:true,vehicle:'motorcycle'},fields:[input('name','Driver full name'),input('phone','Phone (+255…)'),select('vehicle','Vehicle type',options(['motorcycle','Bajaj','van','other'])),input('registration','Vehicle registration'),activeField]})
  const selectOrder=async(row:R)=>{try{setSelected(await api('order',{id:row.id}))}catch(e){setError((e as Error).message)}}
  const rows=Array.isArray(data)?data.filter(r=>JSON.stringify(r).toLowerCase().includes(search.toLowerCase())):[]
  const orderTable=(values:R[])=><Table rows={values} columns={[{label:'Order',render:r=><button onClick={()=>selectOrder(r)}>{r.number}<small>{stamp(r.created_at)}</small></button>},{label:'Recipient',render:r=><>{r.recipient}<small>{r.phone}</small></>},{label:'Delivery',render:r=>badge(r.status)},{label:'Payment',render:r=><>{badge(r.payment_status)}<small>{money(r.total)} · Due {money(r.outstanding)}</small></>},{label:'Action',render:r=><button onClick={()=>selectOrder(r)}>{isStaff?'Process order':'View / receipt'} <ArrowRight size={12}/></button>}]}/>
  const reportLink=(format:string,type=reportType,orderId?:string)=>`/api/platform?${new URLSearchParams({resource:'report',type,format,...range,...(orderId?{id:orderId}:{})})}`
  const customerOrders=()=>go(isStaff?'Orders':'My Orders')
  return <div className={`${shell.dashboard} ${s.app}`}><header className={shell.topbar}><button className={shell.mobileToggle} aria-label="Toggle navigation" onClick={()=>setMenu(!menu)}><Menu/></button><Link className={shell.brand} href="/shop"><span className={shell.brandIcon}><Palmtree/></span><span>NUNGWI <b>SHOP</b></span></Link><div style={{marginLeft:'auto'}} className={s.topActions}><Link href="/shop">Open shop <ArrowRight size={13}/></Link><button aria-label="Account notifications" onClick={()=>setPreview({kind:'updates',values:account?.notifications||[]})}><Bell size={17}/></button><span className={s.role}>{user.role.replace('_',' ')}</span><button onClick={async()=>{await mutate('auth.logout');window.location.href='/login'}}><LogOut size={17}/> Sign out</button></div></header>
    <aside className={`${shell.sidebar} ${menu?shell.sidebarOpen:''}`}><nav className={s.nav} aria-label="Platform navigation">{nav.map(({name,icon:Icon})=><button key={name} className={tab===name?shell.navActive:''} onClick={()=>go(name)}><Icon size={23}/>{name}</button>)}</nav><div className={shell.sidebarArt}><Palmtree/><p>Same Beach<br/><span>More Good Times</span></p><small>NUNGWI, ZANZIBAR</small></div></aside>{menu&&<button className={shell.mobileScrim} aria-label="Close navigation" onClick={()=>setMenu(false)}/>}
    <main className={`${shell.main} ${s.main}`}><div className={s.welcome}><div><h1>{tab==='Overview'?`Jambo, ${user.name.split(' ')[0]}!`:tab}</h1><p>{account?.hotel?account.hotel.name:isStaff?'Your shop, stock and island deliveries.':'Your orders, favourite drinks and delivery details.'}</p></div><div className={s.filters}><select aria-label="Reporting period" value={period} onChange={e=>{setPeriod(e.target.value);if(e.target.value!=='custom'){const end=new Date();const start=new Date();start.setDate(start.getDate()-(Number(e.target.value)-1));setFrom(start.toISOString().slice(0,10));setTo(end.toISOString().slice(0,10))}}}><option value="1">Today</option><option value="7">Last 7 days</option><option value="30">Last 30 days</option><option value="custom">Custom range</option></select>{period==='custom'&&<><input aria-label="From date" type="date" value={from} max={to} onChange={e=>{if(e.target.value)setFrom(e.target.value)}}/><input aria-label="To date" type="date" value={to} min={from} onChange={e=>{if(e.target.value)setTo(e.target.value)}}/></>}</div></div>
      {notice&&<div className={s.notice} role="status">{notice}<button aria-label="Dismiss notification" onClick={()=>setNotice('')}><X size={16}/></button></div>}{error&&<p role="alert" className={s.error}>{error}</p>}
      {loading?<p className={s.empty}>Loading your records…</p>:data&&<>
      {(tab==='Overview'||tab==='Reports')&&<><section className={s.metrics}>{[{label:'Sales revenue',value:money(data.sales),hint:'Orders excluding cancellations/returns, less refunds'},{label:'Payments received',value:money(data.paid),hint:'Net recorded payments for selected orders'},{label:'Outstanding payments',value:money(data.outstanding),hint:'Delivery and payment are separate'},{label:'Orders',value:data.orders,hint:`${data.status?.Pending||0} awaiting action`}].map(metric=><button key={metric.label} className={s.metric} style={{textAlign:'left'}} onClick={customerOrders}><small>{metric.label}</small><strong>{metric.value}</strong><em>{metric.hint}</em></button>)}</section>{isStaff&&<section className={s.metrics}>{[{label:'Registered customers',value:data.customers},{label:'Active hotels',value:data.hotels},{label:'Low / out of stock',value:`${data.lowStock} / ${data.outOfStock}`},{label:'Expiry alerts',value:data.expiring?.length||0}].map(m=><article className={s.metric} key={m.label}><small>{m.label}</small><strong>{m.value}</strong></article>)}</section>}<section className={s.grid}><article className={s.panel}><h2>Sales overview</h2>{Object.keys(data.byDay||{}).length?<div className={s.barChart}>{Object.entries(data.byDay as Record<string,number>).map(([day,value])=><div key={day} title={`${day}: ${money(value)}`}><i style={{height:`${Math.max(2,value/Math.max(...Object.values(data.byDay) as number[])*175)}px`}}/><small>{day.slice(5)}</small></div>)}</div>:<p className={s.empty}>Your first order will appear here. Revenue comes from saved orders.</p>}</article><article className={s.panel}><h2>Order status</h2><div className={s.statusList}>{Object.entries(data.status||{}).map(([status,count])=><div key={status}><i/>{status}<strong>{String(count)}</strong></div>)}</div></article></section>{admin&&<section className={s.grid}><article className={s.panel}><h2>Margin and expenses</h2><p>Delivered-product gross margin: <b>{money(data.grossMargin)}</b></p><p>Recorded expenses: <b>{money(data.expenses)}</b></p><p className={s.hint}>This is not net profit. Delivery costs, commission expenses and all other business expenses must be recorded for a complete profit calculation.</p></article><article className={s.panel}><h2>Hotel commissions · all time</h2><p>Earned: <b>{money(data.commissions?.earned)}</b> · Paid: <b>{money(data.commissions?.paid)}</b></p><p>Balance after refund adjustments: <b>{money(data.commissions?.balance)}</b></p><button className={s.secondary} onClick={()=>go('Commissions')}>Review commissions</button></article></section>}{tab==='Overview'&&<><article className={s.panel}><div className={s.toolbar}><h2>Recent orders</h2><button className={s.secondary} onClick={customerOrders}>View orders <ArrowRight size={15}/></button></div>{orderTable(data.recent||[])}</article><article className={s.panel}><h2>Best-selling products</h2><Table rows={data.bestProducts||[]} columns={[{label:'Product',render:r=>r.name},{label:'Quantity',render:r=>r.quantity},{label:'Gross product sales',render:r=>money(r.sales)}]}/></article>{isStaff&&data.expiring?.length>0&&<article className={s.panel}><h2>Expired and soon-to-expire batches</h2><Table rows={data.expiring} columns={[{label:'Product / batch',render:r=>`${r.name} · ${r.label}`},{label:'Expiry',render:r=>stamp(r.expires_at)},{label:'Remaining / reserved',render:r=>`${r.remaining} / ${r.reserved}`} ]}/></article>}</>}</>}
      {['Orders','My Orders'].includes(tab)&&<article className={s.panel}><div className={s.toolbar}><h2>Order processing</h2><input className={s.search} placeholder="Search order, customer, status…" value={search} onChange={e=>setSearch(e.target.value)}/><Link className={s.primary} href="/shop">Place order</Link></div>{orderTable(rows)}</article>}
      {tab==='Products'&&<article className={s.panel}><div className={s.toolbar}><h2>Product catalogue</h2><input className={s.search} placeholder="Search products…" value={search} onChange={e=>setSearch(e.target.value)}/>{allowed(['admin','stock'])&&<button className={s.primary} onClick={()=>editProduct()}><Plus size={16}/> Add product</button>}</div><Table rows={rows} columns={[{label:'Product',render:r=><><img className={s.thumb} src={r.image} alt=""/>{r.name}<small>{r.sku} · {r.volume} · {r.unit} × {r.unit_size}</small></>},{label:'Prices',render:r=><>{money(r.price)}<small>Hotel: {r.hotel_price===null?'Standard':money(r.hotel_price)}</small></>},{label:'Stock',render:r=><>{r.available} available / {r.reserved} reserved<small>Base units · reorder at {r.reorder_level}</small></>},{label:'Status',render:r=>badge(r.active?'Active':'Archived')},{label:'Action',render:r=>allowed(['admin','stock'])?<button onClick={()=>editProduct(r)}>Edit</button>:null}]}/></article>}
      {tab==='Inventory'&&<><article className={s.panel}><div className={s.toolbar}><h2>Stock batches · base units</h2><button className={s.primary} onClick={()=>setForm({title:'Receive stock',action:'stock.receive',initial:{quantity:24,cost:0,reason:'Supplier delivery',expires_at:new Date(Date.now()+180*86400000).toISOString()},fields:[select('product_id','Product',productOptions),input('label','Batch / supplier reference'),input('quantity','Quantity in base units','number',{min:1}),input('cost','Purchase cost per base unit (TZS)','number'),input('expires_at','Expiry','datetime-local'),input('reason','Reason')]})}>Receive stock</button></div><p className={s.hint}>Reservations prevent overselling. Dispatch uses earliest-expiring stock first. Returned products stay quarantined; receive a new inspected batch to return them to sale.</p><Table rows={data.batches||[]} columns={[{label:'Product / batch',render:r=><>{r.name}<small>{r.label}</small></>},{label:'Expiry',render:r=><>{stamp(r.expires_at)} {r.expires_at<new Date().toISOString()&&badge('expired')}</>},{label:'Remaining / reserved',render:r=>`${r.remaining} / ${r.reserved}`},{label:'Unit cost',render:r=>money(r.cost)},{label:'Adjust',render:r=><button onClick={()=>setForm({title:'Record stock loss',action:'stock.adjust',initial:{batch_id:r.id,kind:'damaged',quantity:1},fields:[select('kind','Reason type',options(['damaged','expired'])),input('quantity','Unreserved base units','number',{min:1,max:r.remaining-r.reserved}),input('reason','Details')]})}>Record loss</button>}]}/></article><article className={s.panel}><h2>Movement history</h2><Table rows={data.movements||[]} columns={[{label:'When / who',render:r=><>{stamp(r.created_at)}<small>{r.actor||'System'}</small></>},{label:'Product',render:r=>r.name},{label:'Movement',render:r=>r.kind},{label:'Base units',render:r=>r.quantity},{label:'Reason',render:r=>r.reason}]}/></article></>}
      {['Customers & Staff','Hotel Staff'].includes(tab)&&<article className={s.panel}><div className={s.toolbar}><h2>Accounts</h2><button className={s.primary} onClick={inviteUser}>Invite user <Plus size={16}/></button></div><Table rows={rows} columns={[{label:'Person',render:r=><>{r.name}<small>{r.email} · {r.phone}</small></>},{label:'Role',render:r=>r.role},{label:'Phone',render:r=>badge(r.phone_verified?'Verified':'Unverified')},{label:'Status',render:r=>badge(r.active===0?'Inactive':r.invited?'Invited':'Active')},{label:'Action',render:r=><button disabled={r.id===user.id} onClick={()=>setForm({title:'Manage account',action:'user.update',initial:{id:r.id,active:r.active!==0,role:r.role},fields:[select('role','Role',options(manager?['hotel_staff']:r.hotel_id?['hotel_manager','hotel_staff']:['admin','sales','stock','delivery','customer'])),activeField]})}>Manage</button>}]}/></article>}
      {tab==='Hotels'&&<><article className={s.panel}><div className={s.toolbar}><h2>Hotels & arrangements</h2><button className={s.primary} onClick={()=>hotelForm()}>Add hotel</button></div><Table rows={rows} columns={[{label:'Hotel',render:r=><>{r.name}<small>{r.contact} · {r.phone} {r.phone_verified?'(verified)':''}</small></>},{label:'Referral',render:r=><>{r.code}<small>{r.rule.scope} · {r.rule.method} {r.rule.value}</small></>},{label:'Purchases / commission balance',render:r=><>{money(r.purchases)}<small>{money(r.balance)}</small></>},{label:'Status',render:r=>badge(r.active?'Active':'Inactive')},{label:'Action',render:r=><button onClick={()=>hotelForm(r)}>Edit arrangement</button>}]}/></article><article className={s.panel}><h2>Commission calculator</h2><div className={s.filters}><input aria-label="Eligible sales" type="number" min="0" value={calculator.eligible} onChange={e=>setCalculator({...calculator,eligible:Number(e.target.value)})}/><select value={calculator.method} onChange={e=>setCalculator({...calculator,method:e.target.value})}><option>percentage</option><option>fixed</option><option>tiered</option></select><input aria-label="Commission value" type="number" min="0" value={calculator.value} onChange={e=>setCalculator({...calculator,value:Number(e.target.value)})}/>{calculator.method==='tiered'&&<input aria-label="Commission tiers JSON" value={calculator.tiers} onChange={e=>setCalculator({...calculator,tiers:e.target.value})}/>}<button className={s.primary} onClick={async()=>{try{const result=await mutate('commission.calculate',{eligible:calculator.eligible,rule:{method:calculator.method,value:calculator.value,scope:'referral',tiers:calculator.method==='tiered'?JSON.parse(calculator.tiers):[]}});setCalculator({...calculator,amount:result.amount})}catch(e){setError((e as Error).message)}}}>Calculate</button><strong>{money(calculator.amount)}</strong></div><p className={s.hint}>Eligible sales exclude discounts, delivery fees, taxes and deposits. Tiers apply to each order’s eligible value. Saving a different rate never changes past orders.</p></article></>}
      {tab==='Drivers'&&<article className={s.panel}><div className={s.toolbar}><h2>Saved drivers · no account needed</h2><button className={s.primary} onClick={()=>driverForm()}>Add driver</button></div><Table rows={rows} columns={[{label:'Driver',render:r=>r.name},{label:'Phone',render:r=>r.phone},{label:'Vehicle',render:r=>`${r.vehicle} · ${r.registration}`},{label:'Status',render:r=>badge(r.active?'Active':'Inactive')},{label:'Action',render:r=><button onClick={()=>driverForm(r)}>Edit</button>}]}/></article>}
      {tab==='Commissions'&&<article className={s.panel}><div className={s.toolbar}><h2>Commission ledger</h2><a className={s.secondary} href={reportLink('pdf','commissions')}>Download statement</a>{admin&&<button className={s.primary} onClick={()=>setForm({title:'Record commission payout',action:'commission.payout',fields:[select('hotel_id','Hotel',hotels.map(h=>({value:h.id,label:`${h.name} · ${money(h.balance)}`}))),input('reference','Actual payment reference') ]})}>Record payout</button>}<button className={s.secondary} onClick={async()=>{try{const values=await api<R[]>('payouts');setPreview({kind:'payouts',values})}catch(e){setError((e as Error).message)}}}>Payout history</button></div><p className={s.hint}>Payable only after delivery and confirmed payment. Refunds reduce earnings; negative balances recover earlier overpayments. Recording a payout does not transfer money.</p><Table rows={rows} columns={[{label:'Order / hotel',render:r=><>{r.number}<small>{r.hotel||account?.hotel?.name}</small></>},{label:'Eligible',render:r=>money(r.eligible)},{label:'Expected',render:r=>money(r.expected)},{label:'Earned / paid',render:r=><>{money(r.earned)}<small>Paid {money(r.paid)}</small></>},{label:'Balance',render:r=>money(r.earned-r.paid)}]}/></article>}
      {tab==='Promotions'&&<article className={s.panel}><div className={s.toolbar}><h2>Promotions & pricing</h2><button className={s.primary} onClick={()=>promotionForm()}>Create promotion</button></div><Table rows={rows} columns={[{label:'Promotion',render:r=><>{r.title}<small>{r.code} · {r.template}</small></>},{label:'Pricing rule',render:r=>`${r.kind==='percentage'?`${r.value}%`:money(r.value)} · ${r.audience}`},{label:'Schedule',render:r=><>{stamp(r.starts_at)}<small>until {stamp(r.ends_at)}</small></>},{label:'Status',render:r=>badge(!r.active?'Inactive':r.ends_at<new Date().toISOString()?'Expired':r.starts_at>new Date().toISOString()?'Scheduled':'Active')},{label:'Actions',render:r=><><button onClick={()=>promotionForm(r)}>Edit</button><button onClick={()=>setPreview(r)}>Desktop / mobile preview</button></>}]}/></article>}
      {tab==='Offers'&&<div className={s.promoGrid}>{rows.map(p=><article key={p.id} className={s.promo} style={{backgroundColor:p.color,backgroundImage:p.image?`linear-gradient(#0005,#0005),url(${p.image})`:undefined}}><h3>{p.title}</h3><p>{p.offer_text}</p><b>{p.code}</b><p>Until {stamp(p.ends_at)}</p><Link href={`/shop?promo=${encodeURIComponent(p.code)}`}>{p.button} <ArrowRight size={15}/></Link></article>)}{!rows.length&&<p className={s.empty}>No offers are active for your account today.</p>}</div>}
      {tab==='Reports'&&<article className={s.panel}><h2>Download reports</h2><div className={s.toolbar}><select className={s.search} value={reportType} onChange={e=>setReportType(e.target.value)}>{(admin?['sales','products','stock','expiry','deliveries','outstanding','commissions']:user.role==='stock'?['stock','expiry']:user.role==='delivery'?['deliveries']:['sales','products','outstanding']).map(type=><option key={type}>{type}</option>)}</select><a className={s.primary} href={reportLink('pdf')}>Download PDF</a><a className={s.secondary} href={reportLink('csv')}>Download CSV</a></div><p className={s.hint}>Reports use the selected dates and your access permissions. CSV files can be opened in Excel. Stock quantities are base units; sales quantities are selling units.</p></article>}
      {tab==='Notifications'&&<article className={s.panel}><div className={s.toolbar}><h2>SMS & email outbox</h2><button className={s.primary} onClick={()=>execute('notifications.process',{},'Notification queue processed. Unconfigured channels stay queued.')}>Process queue</button><button className={s.secondary} onClick={()=>execute('notifications.refresh',{},'SMS statuses reconciled.')}>Refresh SMS statuses</button></div><p className={s.hint}>Queued = waiting. Submitted = provider accepted. Sent/delivered reflect SMS provider status. Unknown outcomes are never retried automatically.</p><Table rows={rows} columns={[{label:'Channel / recipient',render:r=><>{r.channel}<small>{r.recipient}</small></>},{label:'Message',render:r=>r.message},{label:'Status / attempts',render:r=><>{badge(r.status)} · {r.attempts}<small>{r.error}</small></>},{label:'Created',render:r=>stamp(r.created_at)},{label:'Action',render:r=>r.status==='failed'?<button onClick={()=>execute('notifications.retry',{id:r.id},'Message queued for retry.')}>Retry</button>:null}]}/></article>}
      {tab==='Audit'&&<article className={s.panel}><h2>Audit history</h2><Table rows={rows} columns={[{label:'When',render:r=>stamp(r.created_at)},{label:'Who',render:r=>r.actor||'System'},{label:'Action',render:r=>r.action},{label:'Entity',render:r=>`${r.entity} ${r.entity_id}`},{label:'Changes',render:r=><details><summary>View details</summary><pre style={{whiteSpace:'pre-wrap',fontSize:10}}>{JSON.stringify(JSON.parse(r.detail),null,2)}</pre></details>}]}/></article>}
      {tab==='Settings'&&<><article className={s.panel}><h2>Store settings</h2><p className={s.hint}>SMS: {data.smsConfigured?'configured':'not configured'} · Email: {data.emailConfigured?'configured':'not configured'}. Secrets belong in environment variables, never in dashboard forms.</p><PlatformForm action="settings.save" initial={{delivery_fee:6500,free_delivery_threshold:66250,minimum_order:0,tax_percent:0,...data.values}} fields={[input('delivery_fee','Delivery fee TZS','number'),input('free_delivery_threshold','Free delivery threshold TZS','number'),input('minimum_order','Minimum order TZS','number'),input('tax_percent','Tax percentage','number',{max:100})]} onSaved={saved}/></article><article className={s.panel}><div className={s.toolbar}><h2>Recorded expenses</h2><button className={s.primary} onClick={()=>setForm({title:'Record expense',action:'expense.create',initial:{date:new Date().toISOString()},fields:[input('amount','Amount TZS','number',{min:1}),input('description','Description'),input('date','Date','datetime-local')]})}>Add expense</button></div><Table rows={data.expenses||[]} columns={[{label:'Date',render:r=>stamp(r.date)},{label:'Description',render:r=>r.description},{label:'Amount',render:r=>money(r.amount)}]}/></article></>}
      {tab==='Favourites'&&<div className={s.productGrid}>{catalog.filter(p=>data.favourites?.includes(p.id)).map(p=><article className={s.productCard} key={p.id}><img src={p.image} alt={p.name}/><div><h3>{p.name}</h3><p>{money(p.price)} · {p.available} base units available</p><Link className={s.primary} href="/shop">Order from shop</Link><button className={s.secondary} onClick={()=>execute('favourite.toggle',{id:p.id},'Favourite removed.')}>Remove</button></div></article>)}{!data.favourites?.length&&<p className={s.empty}>Save favourites from the shop to find them here.</p>}</div>}
      {tab==='Addresses'&&<article className={s.panel}><div className={s.toolbar}><h2>Your delivery locations</h2><button className={s.primary} onClick={()=>setForm({title:'Add address',action:'address.save',fields:[input('label','Label'),input('address','Address / landmarks','textarea'),input('is_default','Default location','checkbox')]})}>Add location</button></div><Table rows={data.addresses||[]} columns={[{label:'Location',render:r=><>{r.label}{r.is_default?badge('Default'):null}</>},{label:'Address',render:r=>r.address},{label:'Action',render:r=><><button onClick={()=>setForm({title:'Edit address',action:'address.save',initial:{...r,is_default:!!r.is_default},fields:[input('label','Label'),input('address','Address','textarea'),input('is_default','Default location','checkbox')]})}>Edit</button><button onClick={()=>execute('address.delete',{id:r.id},'Address removed.')}>Remove</button></>}]}/></article>}
      {tab==='Account'&&<article className={s.panel}><h2>Your profile</h2><PlatformForm key={revision} action="account.save" initial={{...data.user,notifications:!!data.user.notifications}} fields={[input('name','Full name'),input('phone','International phone (+255…)'),input('notifications','Order SMS notifications','checkbox')]} onSaved={saved}/><div className={s.subForm}><h3>Phone verification</h3>{data.user.phone_verified?badge('Verified'):<><button className={s.secondary} onClick={()=>execute('phone.send',{},'Verification SMS queued.')}>Send verification code</button><PlatformForm action="phone.verify" fields={[input('code','Six-digit code')]} onSaved={saved} submit="Verify phone"/></>}</div><Link className={s.secondary} href="/forgot-password">Reset password by email</Link></article>}
      {tab==='Support'&&<article className={s.panel}><div className={s.toolbar}><h2>Support conversations</h2>{!isStaff&&<button className={s.primary} onClick={()=>setForm({title:'Contact support',action:'support.create',initial:{order_id:null},fields:[input('message','How can we help?','textarea')]})}>New request</button>}</div><Table rows={isStaff?rows:data.support||[]} columns={[{label:'Created',render:r=>stamp(r.created_at)},{label:'Message',render:r=><>{r.message}<small>Order: {r.order_id||'General'}</small></>},{label:'Reply',render:r=>r.reply||'Awaiting response'},{label:'Status',render:r=>badge(r.status)},{label:'Action',render:r=>isStaff?<button onClick={()=>setForm({title:'Reply to customer',action:'support.reply',initial:r,fields:[input('reply','Response','textarea'),select('status','Status',options(['Open','Resolved']))]})}>Reply</button>:null}]}/></article>}
      </>}
    </main>
    <dialog ref={dialog} className={s.dialog} onCancel={close}><div className={s.dialogHeader}><h2>{form?.title||selected?.number||(inviteLink?'Secure invitation':'Preview')}</h2><button aria-label="Close dialog" onClick={close}><X size={18}/></button></div>{form&&<PlatformForm key={form.action+JSON.stringify(form.initial)} {...form} onSaved={saved}/ >}{inviteLink&&<><p className={s.hint}>This single-use invitation expires in 24 hours. An email is queued; delivery requires a configured email provider. Share this link privately with the intended account holder.</p><input readOnly className={s.secureLink} value={inviteLink} onFocus={e=>e.target.select()}/></>}
      {selected&&!form&&<><div className={s.details}>{[['Recipient',selected.recipient],['Contact',selected.phone],['Address',selected.address],['Delivery instructions',selected.instructions||'None'],['Requested window',selected.delivery_window||'As soon as possible'],['Payment',`${selected.payment_status} · ${selected.payment_method}`],['Order total',money(selected.total)],['Outstanding',money(selected.outstanding)]].map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</div>{orderTable([selected])}<Table rows={selected.items||[]} columns={[{label:'Product',render:r=>r.name},{label:'Quantity / unit',render:r=>`${r.quantity} ${r.unit}`},{label:'Price',render:r=>money(r.unit_price)},{label:'Line total',render:r=>money(r.quantity*r.unit_price)}]}/>{selected.driver&&<article className={s.panel}><h3>Delivery assignment</h3><p>{selected.driver.name} · <a href={`tel:${selected.driver.phone}`}>{selected.driver.phone}</a></p><p>{selected.driver.vehicle} · {selected.driver.registration}</p><p>Estimated arrival: {selected.driver.eta}</p><p className={s.hint}>An estimate, not live location tracking.</p></article>}{selected.customer_code&&!['Delivered','Cancelled','Returned'].includes(selected.status)&&<p className={s.hint}>Give this code only after receiving your order: <strong className={s.code}>{selected.customer_code}</strong></p>}<div className={s.toolbar}><a className={s.secondary} href={reportLink('pdf','receipt',selected.id)}>Download receipt</a>{!isStaff&&<><button className={s.primary} onClick={()=>{localStorage.setItem('nungwi-reorder',JSON.stringify(selected.items.map((i:R)=>({id:i.product_id,qty:i.quantity,unit:i.unit}))));window.location.href='/shop'}}>Order again</button><button className={s.secondary} onClick={()=>setForm({title:'Ask about this order',action:'support.create',initial:{order_id:selected.id},fields:[input('message','Message','textarea')]})}>Contact support</button></>}</div><div className={s.history}>{selected.history?.map((h:R)=><div key={h.id}><strong>{h.status}</strong><small>{stamp(h.created_at)} · {h.actor||'Secure driver link'}</small><small>{h.note}</small></div>)}</div>{isStaff&&<><div className={s.toolbar}><button className={s.primary} onClick={()=>setForm({title:'Update order status',action:'order.status',initial:{id:selected.id,status:({Pending:'Confirmed',Confirmed:'Preparing',Preparing:'Out for delivery','Out for delivery':'Delivered','Failed delivery':'Out for delivery',Delivered:'Returned'} as R)[selected.status]||selected.status},fields:[select('status','New status',options(['Confirmed','Preparing','Out for delivery','Delivered','Cancelled','Failed delivery','Returned'])),input('code','Customer confirmation code (delivery only)','text',{required:false}),input('note','Reason / notes','textarea',{required:false})]})}>Update status</button><button className={s.secondary} onClick={()=>setForm({title:'Assign delivery',action:'order.assign',initial:{id:selected.id,instructions:selected.instructions},fields:[select('driver_id','Driver',drivers.filter(d=>d.active).map(d=>({value:d.id,label:`${d.name} · ${d.registration}`}))),input('eta','Estimated arrival / window'),input('instructions','Delivery instructions','textarea',{required:false})]})}>Assign driver</button>{allowed(['admin','sales'])&&<button className={s.secondary} onClick={()=>setForm({title:'Reconcile payment / refund',action:'payment.record',initial:{id:selected.id,amount:selected.outstanding,kind:'payment',method:'cash'},fields:[select('kind','Record type',options(['payment','refund'])),input('amount','Amount TZS','number',{min:1}),select('method','Method',options(['cash','bank_transfer','card','deposit_return'])),input('reference','Actual payment / refund reference')]})}>Record payment / refund</button>}</div><p className={s.hint}>Recording a payment or refund logs an actual transfer; it does not initiate a bank transfer. Delivery requires the customer’s code.</p></>}</>}
      {preview&&(preview.kind==='updates'?<Table rows={preview.values} columns={[{label:'When',render:r=>stamp(r.created_at)},{label:'Update',render:r=>r.message}]}/>:preview.kind==='payouts'?<Table rows={preview.values} columns={[{label:'Date',render:r=>stamp(r.created_at)},{label:'Hotel',render:r=>r.hotel||account?.hotel?.name},{label:'Amount',render:r=>money(r.amount)},{label:'Reference',render:r=>r.reference}]}/>:<><div className={s.previewDevices}>{['Desktop','Mobile'].map(device=><div key={device}><p>{device}</p><article className={s.promo} style={{backgroundColor:preview.color,backgroundImage:preview.image?`linear-gradient(#0004,#0004),url(${preview.image})`:undefined,borderRadius:preview.template==='badge'?'50%':16,aspectRatio:preview.template==='banner'?'auto':'1',maxWidth:preview.template==='badge'?260:undefined}}><h3>{preview.title}</h3><p>{preview.offer_text}</p><strong>{preview.code}</strong><p><Link href={`/shop?promo=${encodeURIComponent(preview.code)}`}>{preview.button}</Link></p></article></div>)}</div><p className={s.hint}>Checkout uses this promotion’s saved dates, audience, linked products and pricing rule.</p></>)}
    </dialog>
  </div>
}
